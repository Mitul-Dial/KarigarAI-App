import 'package:flutter/material.dart';

const kBlue = Color(0xFF2E66FF);       // Palette: Vibrant Blue (Customer Main)
const kBlueDark = Color(0xFF0F172A);   // Palette: Deep Navy Slate
const kBlueLight = Color(0xFFE8EEFF);  // Palette: Light Bright Blue
const kBlueMid = Color(0xFF9CB2FF);    // Palette: Soft Medium Indigo
const kWhite = Colors.white;
const kText = Color(0xFF0F172A);       // Palette: Deep Navy
const kTextMuted = Color(0xFF64748B);  // Slate Gray for clear readable text
const kSurface = Color(0xFFF8FAFC);    // Bright off-white surface
const kBorder = Color(0xFFE2E8F0);     // Light clean border
const kSuccess = Color(0xFF10B981);    // Bright success green
const kGoldenBeige = Color(0xFFD49A3B); // Palette: Bright Honey Gold (Provider Main)
const kWarning = Color(0xFFFBBF24);    // Palette: Soft Warm Yellow

